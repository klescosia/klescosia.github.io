/**
 * The Purple Cloud — themed 404 page
 * Drop-in replacement for quartz/components/pages/404.tsx
 * (keeps the same QuartzComponent exports; only markup + copy change).
 * Styling lives in custom.scss under `.pc-404`.
 */
import { QuartzComponent, QuartzComponentConstructor, QuartzComponentProps } from "../types"

const CLOUD = "M18 10h-1.26A8 8 0 1 0 9 20h9a5 5 0 0 0 0-10z"

const NotFound: QuartzComponent = (_props: QuartzComponentProps) => {
  return (
    <article class="pc-404 popover-hint">
      <div class="pc-404-mark">
        <svg
          width="88"
          height="88"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="1.3"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <path d={CLOUD} />
        </svg>
      </div>
      <h1>404</h1>
      <p class="pc-404-lead">
        This corner of the garden hasn’t grown yet — or the page drifted off into the cloud.
      </p>
      <p>
        <a href="/">← Back to the purple cloud</a>
      </p>
    </article>
  )
}

export default (() => NotFound) satisfies QuartzComponentConstructor
